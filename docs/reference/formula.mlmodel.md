# Extract value formula from mlmodel objects

Extract value formula from mlmodel objects

## Usage

``` r
# S3 method for class 'mlmodel'
formula(x, ...)
```

## Arguments

- x:

  An `mlmodel` object

- ...:

  Currently not implemented
