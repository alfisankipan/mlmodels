% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlmodel-utils.R
\name{se}
\alias{se}
\alias{se.mlmodel}
\title{Extracts standard errors for an \code{mlmodel} object.}
\usage{
se(object, ...)

\method{se}{mlmodel}(
  object,
  vcov = NULL,
  vcov.type = "oim",
  cl_var = NULL,
  repetitions = 999,
  seed = NULL,
  progress = FALSE,
  ...
)
}
\arguments{
\item{object}{An object of class \code{"mlmodel"} or any model that inherits
from it (e.g. \code{"ml_lm"}).}

\item{...}{Not currently used.}

\item{vcov}{An optional user-supplied variance-covariance matrix.}

\item{vcov.type}{Character string specifying the type of variance-covariance
matrix to use. One of \code{"oim"} (default), \code{"robust"}, \code{"opg"}, \code{"cluster"},
or \code{"boot"}.}

\item{cl_var}{Character string or vector. Name of the clustering variable
or the vector itself. Only used when \code{vcov.type = "cluster"}.}

\item{repetitions}{Integer. Number of bootstrap replications to use when
\code{vcov.type = "boot"}. Default is 999.}

\item{seed}{Integer. Random seed for reproducibility when \code{vcov.type = "boot"}.
If \code{NULL}, a random seed is generated.}

\item{progress}{Logical. Should a progress bar be displayed during
bootstrapping? Default is \code{FALSE} (silent) when called from \code{summary()}.}
}
\value{
A named numeric vector of standard errors.
}
\description{
Extracts standard errors for an \code{mlmodel} object.

Extracts standard errors for an \code{mlmodel} object.
}
\seealso{
\link{vcov.mlmodel}
}
\author{
Alfonso Sanchez-Penalver
}
