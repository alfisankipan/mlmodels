% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/probit-postestimation.R
\name{summary.ml_probit}
\alias{summary.ml_probit}
\title{Summary for ml_probit objects}
\usage{
\method{summary}{ml_probit}(
  object,
  correlation = FALSE,
  vcov = NULL,
  vcov.type = "oim",
  cl_var = NULL,
  repetitions = 999,
  seed = NULL,
  progress = FALSE,
  ...
)
}
\arguments{
\item{object}{A fitted model object of class \code{"ml_logit"}.}

\item{correlation}{Logical. Should the correlation matrix of the estimated
parameters be computed and stored in the returned summary object as
\verb{$correlation}? Default is \code{FALSE}. This is provided for compatibility
with \code{maxLik::summary}. The matrix is \strong{not} printed automatically.}

\item{vcov}{Optional user-supplied variance-covariance matrix. If provided,
it will be used instead of computing one internally.}

\item{vcov.type}{Character string specifying the type of variance-covariance
matrix to use. One of \code{"oim"} (default), \code{"robust"}, \code{"opg"}, \code{"cluster"},
or \code{"boot"}. See \code{\link[=vcov.mlmodel]{vcov.mlmodel()}} for details.}

\item{cl_var}{Character string or vector. Name of the clustering variable
or the vector itself. Only used when \code{vcov.type = "cluster"} or when
\code{vcov.type = "boot"} with clustering.}

\item{repetitions}{Integer. Number of bootstrap replications when
\code{vcov.type = "boot"}. Default is 999.}

\item{seed}{Integer. Random seed for reproducibility when \code{vcov.type = "boot"}.
If \code{NULL}, a random seed is generated.}

\item{progress}{Logical. Should a progress bar be displayed during
bootstrapping? Default is \code{FALSE} (silent) when called from \code{summary()}.}

\item{...}{Further arguments passed to methods.}
}
\value{
An object of class \code{summary.ml_probit}.
}
\description{
Summary for ml_probit objects
}
\details{
Coefficient names in the fitted object use the prefixes \verb{value::} and
\verb{scale::} to identify to which equation they belong to, and to avoid
confusion when the same variable(s) appear(s) in both the value and scale
equations.

For binary probit models, two pseudo-R-squared measures are computed and
stored in the \verb{$r.squared} component:
\itemize{
\item \code{cor}: Squared correlation between observed and fitted values.
\item \code{mczav}: McKelvey & Zavoina's pseudo-R².
}
}
