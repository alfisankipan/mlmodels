% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lm-postestimation.R
\name{summary.ml_lm}
\alias{summary.ml_lm}
\title{Summary for ml_lm objects}
\usage{
\method{summary}{ml_lm}(
  object,
  correlation = FALSE,
  vcov = NULL,
  vcov.type = "oim",
  cl_var = NULL,
  repetitions = 999,
  seed = NULL,
  progress = FALSE,
  ...
)
}
\arguments{
\item{object}{An object of class \code{"ml_lm"}.}

\item{correlation}{Logical. Should the correlation matrix of the parameters be shown?
Default is \code{FALSE}.}

\item{vcov}{Optional user-supplied variance-covariance matrix.
If provided, it will be used instead of computing one internally.
Must be a square matrix with dimensions matching the number of
coefficients in the model. Useful when you have pre-computed a
bootstrap or clustered variance and want to reuse it.}

\item{vcov.type}{Character string specifying the type of variance-covariance
matrix to use. One of \code{"oim"} (default), \code{"robust"}, \code{"opg"}, \code{"cluster"},
or \code{"boot"}. See \code{\link[=vcov.mlmodel]{vcov.mlmodel()}} for details.}

\item{cl_var}{Character string or vector. Name of the clustering variable
or the vector itself. Only used when \code{vcov.type = "cluster"} or when
\code{vcov.type = "boot"} with clustering.}

\item{repetitions}{Integer. Number of bootstrap replications when
\code{vcov.type = "boot"}. Default is 999.}

\item{seed}{Integer. Random seed for reproducibility when \code{vcov.type = "boot"}.
If \code{NULL}, a random seed is generated.}

\item{progress}{Logical. Should a progress bar be displayed during
bootstrapping? Default is \code{FALSE} (silent) when called from \code{summary()}.}

\item{...}{Further arguments passed to methods.}
}
\value{
An object of class \code{"summary.ml_lm"}.
}
\description{
Summary for ml_lm objects
}
