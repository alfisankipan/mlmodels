% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlmodels-auxiliary.R
\name{get_vcov}
\alias{get_vcov}
\title{Internal helper to obtain the variance-covariance matrix}
\usage{
get_vcov(
  object,
  vcov = NULL,
  vcov.type = "oim",
  cl_var = NULL,
  repetitions = 999,
  seed = NULL,
  progress = FALSE,
  ...
)
}
\arguments{
\item{object}{An \code{mlmodel} object.}

\item{vcov}{An optional user-supplied variance-covariance matrix.
If provided, it will be used instead of computing a new one.}

\item{vcov.type}{Character string specifying the type of variance-covariance
matrix to compute. One of \code{"oim"} (default), \code{"robust"}, \code{"opg"},
\code{"cluster"}, or \code{"boot"}.}

\item{cl_var}{Character string or vector. Clustering variable.
Only used when \code{vcov.type = "cluster"}.}

\item{repetitions}{Integer. Number of bootstrap replications when
\code{vcov.type = "boot"}. Default is 999.}

\item{seed}{Integer. Random seed for reproducibility when \code{vcov.type = "boot"}.
If \code{NULL}, a random seed is generated.}

\item{progress}{Logical. Should a progress bar be displayed during
bootstrapping? Default is \code{FALSE}.}

\item{...}{Further arguments passed to \code{\link[=vcov.mlmodel]{vcov.mlmodel()}}.}
}
\value{
A variance-covariance matrix.
}
\description{
This helper is used by \code{summary.ml_lm()}, \code{waldtest.mlmodel()},
\code{predict.ml_lm()} and other functions.
}
\details{
If a matrix is supplied via the \code{vcov} argument, it is validated and
returned directly. Otherwise, it calls \code{vcov.mlmodel()}.
}
\keyword{internal}
